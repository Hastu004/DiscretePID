This folder contains the original thesis-era sketches and companion tooling kept for historical reference.

The maintained Arduino library lives in `src/`, and the supported usage examples live in `examples/`.
